SCC0640 - Bases de Dados - Projeto Copa do Mundo FIFA

MEMBROS DO GRUPO:
- João Marcelo Moreira Trovão Filho - 13676332
- André Luiz Santos Messias        - 15493857
- Mateus Santos Messias            - 12548000
- Pedro Borges Gudin               - 12547997

REQUISITOS:
- Docker Desktop (PostgreSQL e Ollama em containers)
- Python 3.10+ com pip

COMO EXECUTAR (todos os comandos a partir da pasta da entrega, onde estão os 8 arquivos):

1) Descompactar o protótipo:
   - Extrair 07.Prototipo.zip; será criada a pasta "prototipo/".

2) Subir o PostgreSQL e criar o banco:
   docker run -d --name postgres-copa -e POSTGRES_PASSWORD=copa123 -e POSTGRES_DB=copa_mundo -p 5432:5432 postgres:16

3) Carregar DDL e DML:
   docker cp 05.DDL.sql postgres-copa:/tmp/DDL.sql
   docker cp 06.DML.sql postgres-copa:/tmp/DML.sql
   docker exec -it postgres-copa psql -U postgres -d copa_mundo -f /tmp/DDL.sql
   docker exec -it postgres-copa psql -U postgres -d copa_mundo -f /tmp/DML.sql

4) Subir o Ollama e baixar o modelo:
   docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
   docker exec -it ollama ollama pull qwen2.5-coder:3b

   (Se estiver em um Mac com Apple Silicon e quiser mais velocidade na IA,
    instale o Ollama nativo no Mac em vez do container: https://ollama.com/download.
    O modelo é o mesmo: ollama pull qwen2.5-coder:3b. O Docker Desktop não
    repassa a aceleração Metal do macOS para o container Linux.)

5) Instalar dependências Python e rodar o protótipo:
   cd prototipo
   pip install -r requirements.txt
   python main.py

6) Dados de login no protótipo:
   Host: localhost   Porta: 5432   Banco: copa_mundo
   Usuário: postgres   Senha: copa123

OBSERVAÇÕES / O QUE NÃO FOI REALIZADO:
- O DML cobre as Copas de 1998, 2002, 2006, 2010, 2014, 2018 e 2022, todas com
  32 seleções e máximo de 7 jogos por seleção. A Copa de 2026 foi excluída por
  ter outro formato (48 seleções).
- O modo "[IA] Consulta em linguagem natural" usa o modelo qwen2.5-coder:3b via
  Ollama. O SQL gerado é sempre exibido antes da execução, exigindo confirmação.
- Para perguntas fora do escopo do banco (ex.: "faça um poema"), a IA retorna
  uma mensagem amigável dizendo que não pode responder.
- Se o PostgreSQL rejeitar o SQL gerado, o protótipo tenta auto-correção até
  3 vezes enviando o erro de volta à IA.
- A opção "Digitar SQL diretamente" aceita múltiplos comandos separados por ";".
